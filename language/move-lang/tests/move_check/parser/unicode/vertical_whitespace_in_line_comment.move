address 0x42 {
  // This line contains a `U+000D CARRIAGE RETURN`<
  module M { } so if line comments are terminated only<
               with `U+000A LINE FEED` then this text<
               would be part of the comment as well
}
